import React from 'react'
import { render } from '@testing-library/react'
import FormError from '../components/errors/FormError'

describe('Form error', () => {
  it('test error message props', () => {
    // const { getByText } = render(<FormError errorMessage={'test'} />)
    // getByText('test')
  })
})
