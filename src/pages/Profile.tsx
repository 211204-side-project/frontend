import React from 'react'
import styled from 'styled-components'

const ProfileSection = styled.section`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
`

const Profile = () => {
  return <ProfileSection>Profile</ProfileSection>
}

export default Profile
